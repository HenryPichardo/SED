@model IEnumerable<SED.Models.Asignatura_Docente>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Asignatura Docente</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Asignatura.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Docentes.codigo_empleado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Asignatura.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Docentes.codigo_empleado)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_asignatura_docente }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_asignatura_docente }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.id_asignatura_docente })
        </td>
    </tr>
}

</table>
