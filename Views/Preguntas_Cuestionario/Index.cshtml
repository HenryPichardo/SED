@model IEnumerable<SED.Models.Preguntas_Cuestionario>

@{
    ViewBag.Title = "Preguntas Cuetionario";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Preguntas Cuestionario</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pregunta.nombre_pregunta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pregunta.nombre_pregunta)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_cuestionario }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_cuestionario }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.id_cuestionario })
        </td>
    </tr>
}

</table>
